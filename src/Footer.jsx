import styles from "./Footer.module.css"

const Footer = () => {
    return <div>
        <h4 className={styles.title}>Este es el footer</h4>
        <h5 className={styles.titleDos}>TitleDos</h5>
    </div>
}